sources:
  - provider: twitter
  - provider: git

options:
  - type: object
    name: siteMetadata
    label: SiteMetadata
    options:
      - type: string
        default: Bloga
        name: siteTitle
        label: Site Title
      - type: string
        default: https://test.com
        name: site_ url
        label: Site URL
  - type: plugin-options
    plugin: gatsby-plugin-google-analytics
    label: Gatsby Plugin Google Analytics
    options:
      - type: string
        name: trackingId
        label: Tracking Id
